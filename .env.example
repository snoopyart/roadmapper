# ===========================================
# LOCAL DEVELOPMENT
# ===========================================
# DATABASE_URL=postgresql://roadmapper:roadmapper_dev@localhost:5432/roadmapper
# JWT_SECRET=dev-secret-change-in-production
# JWT_EXPIRES_IN=7d
# PORT=3001
# NODE_ENV=development
# CLIENT_URL=http://localhost:5173

# ===========================================
# COOLIFY PRODUCTION
# Copy these to your Coolify environment variables
# ===========================================

# Database - use your Coolify PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://roadmapper:YOUR_PASSWORD@your-postgres-host:5432/roadmapper

# Authentication - generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32
JWT_EXPIRES_IN=7d

# Server
PORT=3001
NODE_ENV=production

# Database SSL - set to 'true' only if your PostgreSQL requires SSL
# For Coolify internal PostgreSQL connections, leave this unset or 'false'
# DATABASE_SSL=false

# CORS - set to your domain (optional, defaults to same origin in production)
# CLIENT_URL=https://your-domain.com
